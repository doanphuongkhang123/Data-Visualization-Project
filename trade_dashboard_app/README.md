# Global Trade Dashboard

First-tab prototype inspired by the WTO merchandise trade dashboard.

## Run

```bash
python3 -m streamlit run trade_dashboard_app/app.py
```

The app reads:

```text
new/trade_volume_annual.csv
```
